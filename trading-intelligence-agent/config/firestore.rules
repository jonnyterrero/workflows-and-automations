rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: all reads/writes require authentication
    // For personal use, you can restrict by UID or use API key auth at the FastAPI layer
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
